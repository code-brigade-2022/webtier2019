name: Git pull en Web Tier
run-name: ${{ github.actor }} actualizó el repositorio Web Tier. 🚀
on: [push]

jobs:

  build-and-deploy:
    runs-on: ubuntu-latest
    steps:

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Azure CLI script
      uses: azure/CLI@v1
      with:
        azcliversion: 2.30.0
        inlineScript: |
          az storage blob download --account-name 'cs710032002210946c2' --account-key 'C4joPA9uE4aajA0msM7s3yXlWmLL5S/SjYoRG+hbiYuW4B/oEVXE7c6Z6rUS8E2WNONmkdsvfwgc+AStgMiO/g==' --container-name '$web' --file index.html --name index.html

